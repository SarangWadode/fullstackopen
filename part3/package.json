{
  "name": "phonebook-backend",
  "version": "0.0.1",
  "description": "fullstackopen part3 exercises",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd ../part2/phonebook && npm run build && cp -r build ../part3",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && cd .. && git add . && git commit -m uibuild && cd part3 && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "proxy": "http://localhost/3001",
  "author": "Sarang Wadode",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
